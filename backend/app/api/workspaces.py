"""Workspace router (create / list / inspect connected databases).

Stub for Wave 1.
"""
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/workspaces", tags=["workspaces"])
