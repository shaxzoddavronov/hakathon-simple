"""Schema router (fetch / refresh the profiled schema bundle).

Stub for Wave 1.
"""
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/schema", tags=["schema"])
