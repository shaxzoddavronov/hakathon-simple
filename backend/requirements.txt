# QueryMind AI backend runtime + dev dependencies.
# Pin only major/minor floors so the resolver can pick compatible patch versions.

# Web framework
fastapi
uvicorn[standard]
# Required by FastAPI for OAuth2 form login (/auth/login) and EmailStr fields
python-multipart
email-validator

# ORM + drivers
sqlalchemy[asyncio]>=2.0
asyncpg
aiosqlite
alembic
# Data-source engine drivers (user-connected DBs)
clickhouse-connect[async]

# Config / validation
pydantic>=2
pydantic-settings

# Auth / crypto
python-jose[cryptography]
passlib[bcrypt]
# passlib 1.7.4's bcrypt handler is incompatible with bcrypt >=4.1: its internal
# wrap-bug probe trips bcrypt's new 72-byte ValueError, which 500s every
# register/login. Cap until passlib ships a release supporting modern bcrypt.
bcrypt<4.1
cryptography

# Background jobs
celery[redis]
redis

# SQL parsing (used by the read-only validator)
sqlglot

# HTTP client + OpenAI-compatible vLLM client
httpx
openai

# Agent orchestration
langgraph
langchain-core

# Dev / test
pytest
pytest-asyncio
pytest-cov
